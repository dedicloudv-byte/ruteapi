{
  "name": "api-relay-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check SRC/worker.js"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
